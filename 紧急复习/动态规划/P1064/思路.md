多重背包
```
	for(int i = 1; i <= m; i++){
		for(int j = n; j >= 0; j--){
			for(int sl = 0; sl < 4; sl++){
				q = que[sl][i];
				if(q == 0 || j < q){
					continue;
				}
				v = value[sl][i];

				dp[j] = max(dp[j], dp[j - q] + v);
			}
		}
	}
```
