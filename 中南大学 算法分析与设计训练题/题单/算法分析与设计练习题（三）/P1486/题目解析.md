# P1486 [NOI2004] 郁闷的出纳员

## 题目信息

- **题目链接**：https://www.luogu.com.cn/problem/P1486
- **知识点**：平衡树、延迟全局偏移、第 k 大查询
- **时间限制**：1.00s
- **内存限制**：125MB

## 题目简述

> 维护一个员工工资集合，支持四种操作：插入新员工（工资低于下界不计入）、全体员工加工资、全体员工扣工资（低于下界者离开）、查询第 $k$ 高的工资。输出每次查询结果以及最终离开公司的员工总数。
>
> 数据范围：$n \le 3 \times 10^5$，插入和查询命令数均不超过 $10^5$，加减工资命令总数不超过 $100$。

## 题目分析

### 详细版

**1. 读题**

操作数量大，但全局加减操作只有不超过 $100$ 次，因此可以用一个全局偏移量 `delta` 来延迟处理所有加减工资操作，避免对集合中每个元素都修改。

**2. 全局偏移技巧**

- 维护一个全局变量 `delta`，表示所有员工实际工资与存储值之间的差值。
- `A k`：`delta += k`。
- `S k`：`delta -= k`，然后把实际工资低于 `min` 的员工删除，即删除存储值小于 `min - delta` 的元素。
- `I k`：如果 `k >= min`，将 `k - delta` 插入集合；否则忽略（不计入离开人数）。
- `F k`：查询第 $k$ 大，等价于集合中第 `(size - k + 1)` 小的元素，输出时加上 `delta`。

**3. 数据结构**

需要支持动态插入、删除小于某值的所有元素、第 $k$ 小查询。使用 GNU PBDS 的 `tree`（顺序统计树），以 `(值, 唯一 id)` 作为键，支持 `lower_bound` 和 `find_by_order`，所有操作 $O(\log n)$。

**4. 时空复杂度分析**

- 时间复杂度：$O((n + \text{删除总数}) \log n)$。
- 空间复杂度：$O(n)$。

### 省流版

用全局偏移量 `delta` 维护加减工资，实际存储相对工资；用 PBDS 顺序统计树支持插入、批量删除低工资和第 $k$ 大查询。

## 代码实现

![AC截图](./P1486AC.png)

```cpp
// P1486 [NOI2004] 郁闷的出纳员
/*
链接：https://www.luogu.com.cn/problem/P1486
知识点：平衡树、延迟全局偏移、第 k 大查询
*/
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stack>
#include <string>
#include <queue>
#include <vector>
#include <list>
#include <map>
#include <set>
#include <algorithm>
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>

using namespace std;
using namespace __gnu_pbds;

typedef tree<pair<long long, int>, null_type, less<pair<long long, int>>, rb_tree_tag, tree_order_statistics_node_update> OrderSet;

OrderSet s;

int main(){
    int n;
    long long minv;
    scanf("%d%lld", &n, &minv);

    long long delta = 0;
    int left = 0;
    int id = 0;

    for(int i = 0; i < n; i++){
        char op[2];
        long long k;
        scanf("%s%lld", op, &k);
        if(op[0] == 'I'){
            if(k >= minv){
                s.insert({k - delta, ++id});
            }
        }else if(op[0] == 'A'){
            delta += k;
        }else if(op[0] == 'S'){
            delta -= k;
            long long threshold = minv - delta;
            auto it = s.lower_bound({threshold, 0});
            while(it != s.begin()){
                --it;
                s.erase(it++);
                left++;
            }
        }else if(op[0] == 'F'){
            int sz = (int)s.size();
            if(k > sz){
                printf("-1\n");
            }else{
                auto it = s.find_by_order(sz - k);
                printf("%lld\n", it->first + delta);
            }
        }
    }

    printf("%d\n", left);
    return 0;
}
```

## 代码链接

代码发布于：https://github.com/Flutter-Misdreavus/csu_algorithm
