回忆怎么写并查集
```
int find_bi(int a){
	if(a == bingchaji[a]){
		return a;
	}

	return find_bi(a);
}
```

```
void merge_bi(int a, int b){
	a = find_bi(a);
	b = find_bi(b);
	bingchaji[b] = a;
	return;
}
```
