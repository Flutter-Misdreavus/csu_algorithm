树形DP
dp[i]代表以i为根的最大子树

```
void dfs(int dian, int father){
	dp[dian] = flower[dian];

	for(auto& i : bian[dian]){
		if(i == father){
			continue;
		}

		dfs(i, dian);
		dp[dian] += max(0, dp[i]);
	}

	maxflow = max(dp[dian], maxflow);
}
```
