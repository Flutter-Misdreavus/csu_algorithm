enemy[i]代表以i为根节点的集合是否有敌军
先从大边开始找，逆向思维
```
if(find_bi(a) != find_bi(b) && enemy[find_bi(a)] == true && enemy[find_bi(b)] == true){
// 如果两个点不在同一集合且都有敌军，则不能合并
	continue;
}else{
	merge_bi(a, b);
	daijia += c;
}
```
